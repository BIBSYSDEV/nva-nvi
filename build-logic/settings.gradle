dependencyResolutionManagement {
    versionCatalogs {
        // Other versions defined in this repository
        create("libs", { from(files("../gradle/libs.versions.toml")) })
    }
}

rootProject.name = 'build-logic'
