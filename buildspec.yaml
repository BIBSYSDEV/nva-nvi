version: 0.2

env:
  variables:
    #    GRADLE_USER_HOME: "/root/.gradle"
    GRADLE_OPTS: "-Dorg.gradle.configuration-cache=false"
    SAM_CLI_TELEMETRY: "0"
  secrets-manager:
    DOCKER_HUB_USERNAME: DockerHubCredentials:DockerHubUsername
    DOCKER_HUB_SECRET: DockerHubCredentials:DockerHubSecret

#cache:
#  paths:
#    - "/root/.gradle/caches/**/*"

phases:
  install:
    runtime-versions:
      java: corretto21

    commands:
      - pip3 install --upgrade "aws-sam-cli==1.160.1"
      - sam --version

  pre_build:
    commands:
      # We need to be logged in to Docker Hub to pull images for testcontainers
      - echo "Logging in to Docker Hub..."
      - echo -n "$DOCKER_HUB_SECRET" | docker login --username $DOCKER_HUB_USERNAME --password-stdin

  build:
    commands:
      - echo "=== Pre-build environment ==="
      - df -h
      - free -m
      - nproc
      - du -sh /root/.gradle/caches 2>/dev/null || echo "no gradle cache"
      - docker info 2>&1 | head -20
      # Step-by-step gradle timing on a cold cache. Each step adds the
      # minimum-needed work to the cache, so the wall-clock time reported by
      # `time` for each step is roughly the marginal cost of that phase.
      - echo "=== STEP 1/5: gradle help (startup + plugin resolution only) ==="
      - time ./gradlew help --console=plain
      - echo "=== STEP 2/5: gradle dependencies (resolve + download every config) ==="
      - time ./gradlew dependencies --refresh-dependencies --console=plain
      - echo "=== STEP 3/5: gradle assemble (compile + package, no tests) ==="
      - time ./gradlew assemble --console=plain
      - echo "=== STEP 4/5: gradle test (run all tests) ==="
      - time ./gradlew test --console=plain
      - echo "=== STEP 5/5: sam build (cache fully warm; should be quick) ==="
      # Background sampler so the silent 30+ min gradle invocation inside
      # lambda-builders becomes observable. Logs CPU/network/cache state every 15s.
      - |
        (
          while sleep 15; do
            echo "----- STATS $(date '+%H:%M:%S') -----"
            echo "load: $(cat /proc/loadavg)"
            ps -eo pid,pcpu,pmem,etime,comm --sort=-pcpu | head -8
            ss -tn state established 2>/dev/null | head -10 || true
            du -sh /root/.gradle/caches 2>/dev/null || true
          done
        ) &
        STATS_PID=$!
        time sam build --debug
        kill $STATS_PID 2>/dev/null || true
      - echo "=== Post-build environment ==="
      - df -h
      - du -sh .aws-sam 2>/dev/null || true
      - du -sh /root/.gradle/caches 2>/dev/null || true

  post_build:
    commands:
      - sam package --s3-bucket $S3_BUCKET --output-template-file packaged_raw.yaml
      # Use timestamp to force update of ApiGatewayCreateDeploymentCustomResource
      - BUILD_TIMESTAMP=$(date '+%s')
      - envsubst '${BUILD_TIMESTAMP}' < packaged_raw.yaml > packaged.yaml

artifacts:
  files:
    - packaged.yaml
